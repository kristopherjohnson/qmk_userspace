// Share the rev8 keymap's feature config (split-state sync, Caps Word, RGB
// defaults) so the two boards stay in lockstep. Only board-specific hardware
// config would belong here, and there is none.
#include "../../../../iris/rev8/keymaps/kristopherjohnson/config.h"
